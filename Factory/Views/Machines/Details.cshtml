@{
  Layout = "_Layout";
}

@using Factory.Models;
@model Factory.Models.Machine;

<h1>Machine Details</h1>
<hr />
<h2>@Html.DisplayNameFor(model => model.MachineType): @Html.DisplayFor(model => model.MachineType)</h2>

@if(@Model.Engineers.Count() == 0)
{
  <p>no engineers assigned</p>
}
else
{
  <h4>Engineers:</h4>
  <ul>
    @foreach(var join in Model.Engineers)
    {
    <li>@Html.ActionLink($"{join.Engineer.EngineerName}", "Details", "Engineers", new { id = @join.Engineer.EngineerId })</li> 
    }
  </ul>
}

<p>@Html.ActionLink("Edit machine", "Edit", new { id = Model.MachineId })</p>
<p>@Html.ActionLink("Delete machine", "Delete", new { id = Model.MachineId })</p>
<p>@Html.ActionLink("machine list", "Index")</p>
<p>@Html.ActionLink("Home", "Index", "Home")</p>